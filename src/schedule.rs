use alloc::boxed::Box;

use crate::kobject::Thread;
use crate::switch::switch;

pub fn schedule() {
    // 1. get current thread
    // 2. get next thread
    // 3. context switch to next thread
    //      a.

    // context_switch(curr, next)
}


